# G.4 README Current Portfolio Status

This document summarizes the G.4 README rewrite for VisionCommand AI.

## Purpose

G.4 updates the repository front page so that recruiters, interviewers, and technical reviewers understand the project quickly and accurately.

The README should now lead with the current local Docker demo path instead of old Render links.

## What changed

The README now explains:

- VisionCommand AI as a full-stack applied AI media assistant
- local Docker as the current primary demo path
- historical Render deployment as evidence, not a guaranteed active live demo
- implemented image, video, command, LLM, RAG-style analysis memory, persistence, Docker, and CI/CD capabilities
- User Mode and Developer Mode
- Docker quickstart and smoke checks
- model artifact behavior
- local non-Docker setup
- environment and secrets rules
- testing and validation commands
- architecture and tech stack
- key documentation links
- current limitations
- Milestone G roadmap status
- portfolio positioning

## Current demo claim

Safe current claim:

```text
VisionCommand AI currently supports a local Docker demo for the full application stack.
```

Safe historical deployment claim:

```text
VisionCommand AI was previously deployed on Render as a controlled public cloud demo, and the evidence is documented in the repository.
```

Unsafe claim:

```text
VisionCommand AI has a guaranteed active production cloud deployment.
```

## Boundary

G.4 does not add new product features.

G.4 does not create a cloud deployment.

G.4 does not add screenshots or diagrams.

G.4 does not tag a release.

G.4 does not claim v1 readiness.

G.4 updates the README so the repository front door matches the current Milestone G strategy.
